# Prevent direct access to data files
<FilesMatch "\.(json|log|txt)$">
    Require all denied
</FilesMatch>

# Also for older Apache versions
<Files ~ "\.(json|log|txt)$">
    Order allow,deny
    Deny from all
</Files>
